# Commands, Moved

All workflow files have been moved to `workflows/` at the workspace root.

This workspace is now managed by **Cowork**. Workflow definitions live in `workflows/` and are written in plain-language Cowork format.

If you need to restore Claude Code slash commands, copy the files from `workflows/` back here, the content and logic are identical, just with Claude Code-specific syntax (`$ARGUMENTS`, slash command references) replaced by plain English.
