feat: make delegation dynamic and add 1:1 prep workflow

- scripts/build-action-items-artifact.js: add readTeamMembers() — reads
  team/ at build time, extracts first name from each person file's heading,
  embeds the list in the seed; empty team/ = delegate disabled
- templates/action-items-artifact.template.html: TEAM_MEMBERS now read
  from embedded seed instead of hardcoded; Delegate button, pill, stat
  tile, and delegated section all hidden when no team members exist (and
  no existing delegated items)
- workflows/1on1-prep.md: new workflow — pulls active projects, open
  action items, and delegated items for the named person; includes
  post-meeting reminder to close out delegated work
- CLAUDE.md: register 1on1-prep in workflow trigger table
